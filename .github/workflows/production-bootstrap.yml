name: Mashareena Production Bootstrap (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Disabled: production signing must use repository Actions secrets."
          exit 1
