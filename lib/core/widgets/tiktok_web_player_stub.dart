import 'package:flutter/widgets.dart';

class TikTokWebPlayer extends StatelessWidget {
  final String url;
  const TikTokWebPlayer({super.key, required this.url});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
