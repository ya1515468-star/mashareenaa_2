import '../../domain/entities/username_credential_entity.dart';

class UsernameCredentialModel extends UsernameCredentialEntity {
  const UsernameCredentialModel({
    required super.uid,
    required super.username,
    required super.createdAt,
  });
}
