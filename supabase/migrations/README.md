# Supabase migrations

This directory contains database migrations intended for the Mashareena project.

Production configuration values, service-role credentials, authentication secrets, device-local paths, and internal audit reports are intentionally excluded from this public source tree.

Sensitive server credentials are provided to Edge Functions through environment variables and are never committed to source control.
